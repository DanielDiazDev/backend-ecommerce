using EcommerceProject.Model;

namespace EcommerceProject.Data.Repositories.Contracts;

public interface ICartRepository
{
    // Task<bool> Add(Cart cart);
    // Task<Cart> Get(Guid cartId);
    // bool Update(Cart cart);
}